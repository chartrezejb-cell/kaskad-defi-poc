{
  "name": "kaskad-swap-deploy",
  "version": "1.0.0",
  "description": "Deploy Uniswap V2 Factory + Router to Igra Galleon Testnet",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network galleon",
    "verify-deploy": "npx hardhat run scripts/verify.js --network galleon"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  },
  "dependencies": {
    "@uniswap/v2-core": "^1.0.1",
    "@uniswap/v2-periphery": "^1.1.0-beta.0"
  }
}
